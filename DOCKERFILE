FROM python:3.8

# Set the working directory
WORKDIR /app

# Install dependencies
RUN pip install torch transformers

# Copy the local files
COPY run_chatgpt.py /app/

# Run the Python script
CMD ["python", "run_chatgpt.py"]
